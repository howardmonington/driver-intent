version: "3.8"
services:
  pytorch-jupyter:
    build:
      context: .
      dockerfile: Dockerfile
    runtime: nvidia
    shm_size: '2g'
    ports:
      - 8888:8888
    command: jupyter notebook --ip 0.0.0.0 --no-browser --allow-root
    volumes:
      - ../:/workspace/notebooks
